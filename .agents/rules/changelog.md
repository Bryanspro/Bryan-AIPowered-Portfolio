When updating the CHANGELOG.md file, you MUST also update the changelog entries within the portfolio's UI (index.html).
There is a changelog tab viewer in the HTML, and any new version releases or changes should be synchronized between the two files.
